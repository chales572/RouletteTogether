# 빠른 시작 가이드 (Netlify 없이 사용)

이 PC에서 서버와 클라이언트를 모두 실행하는 방법입니다.

## 🚀 한 번에 시작하기

### 1단계: 서버 시작

```bash
cd server
start-server.bat
```

또는 명령어로:
```bash
cd server
npm start
```

### 2단계: 클라이언트 시작

```bash
cd client
start-client.bat
```

또는 명령어로:
```bash
cd client
node serve.js
```

## ✅ 접속하기

서버와 클라이언트가 모두 실행되면:

### 이 PC에서 접속
- 브라우저에서 http://localhost:5173 열기

### 같은 네트워크의 다른 기기에서 접속
- 브라우저에서 http://10.145.164.94:5173 열기
- (스마트폰, 태블릿, 다른 PC 등)

## 📋 실행 중인 서비스

| 서비스 | 포트 | 로컬 주소 | 네트워크 주소 |
|--------|------|-----------|---------------|
| 서버 (API/Socket) | 3000 | http://localhost:3000 | http://10.145.164.94:3000 |
| 클라이언트 (웹) | 5173 | http://localhost:5173 | http://10.145.164.94:5173 |

## 🔧 문제 해결

### 포트가 이미 사용 중인 경우

**서버 (포트 3000)**:
```bash
netstat -ano | findstr :3000
taskkill /PID <PID번호> /F
```

**클라이언트 (포트 5173)**:
```bash
netstat -ano | findstr :5173
taskkill /PID <PID번호> /F
```

### 다른 기기에서 접속이 안 되는 경우

1. **방화벽 확인** - Windows 방화벽에서 포트 열기:
```bash
netsh advfirewall firewall add rule name="Roulette Server" dir=in action=allow protocol=TCP localport=3000
netsh advfirewall firewall add rule name="Roulette Client" dir=in action=allow protocol=TCP localport=5173
```

2. **IP 주소 확인** - 현재 PC의 IP 주소 확인:
```bash
ipconfig
```
`IPv4 주소`를 찾아서 해당 주소로 접속

## 🛑 서버 종료하기

1. 서버 창에서 `Ctrl + C`
2. 클라이언트 창에서 `Ctrl + C`

또는 Task Manager에서 Node.js 프로세스 종료

## 💡 팁

### Windows 시작 시 자동 실행

1. `Win + R` → `shell:startup` 입력
2. 시작 프로그램 폴더 열림
3. `server/start-server.bat` 바로가기 복사
4. `client/start-client.bat` 바로가기 복사

### 개발 모드로 실행 (코드 수정 시 자동 재시작)

**서버**:
```bash
cd server
npm run dev
```

**클라이언트**:
```bash
cd client
npm run dev
```

## 📱 모바일에서 접속하기

1. 스마트폰/태블릿을 PC와 같은 Wi-Fi에 연결
2. 브라우저에서 http://10.145.164.94:5173 접속
3. 방 만들기 또는 참가하기

## 🌐 외부에서 접속하기 (선택사항)

집 밖에서도 접속하려면:

1. **공인 IP 확인**: https://www.whatismyip.com/
2. **공유기 포트포워딩 설정**:
   - 외부 포트 3000 → 내부 IP 10.145.164.94:3000
   - 외부 포트 5173 → 내부 IP 10.145.164.94:5173
3. **접속**: http://<공인IP>:5173

⚠️ 보안 주의: 외부 접속 시 DDOS 공격 등에 주의하세요.

## 📚 더 자세한 정보

- 서버 설정: [SERVER_SETUP.md](SERVER_SETUP.md)
- 문제 해결: [TROUBLESHOOTING.md](TROUBLESHOOTING.md)
